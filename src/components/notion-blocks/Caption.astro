---
import { RichText } from "../../lib/interfaces.ts";

export interface Props {
	richTexts: RichText[];
}

const { richTexts } = Astro.props;
---

{
	richTexts.length > 0 && richTexts[0].Text.Content && (
		<div class="caption pt-1">{richTexts[0].Text.Content}</div>
	)
}
